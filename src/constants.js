export const BASE_URL = import.meta.env.VITE_BASE_URL;

export const ENDPOINTS = {
  USERS: '/users',
  POSTS: '/posts',
};

const constants = {
  BASE_URL,
  ENDPOINTS,
};

export default constants;
